# Entidad principal: cola de prioridad basada en montículo binario (binary heap)
# Permite inserción O(log n) y extracción del elemento de mayor prioridad O(log n)
# Desempate por tiempo de llegada (FIFO dentro del mismo nivel de prioridad)
